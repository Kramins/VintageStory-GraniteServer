meta {
  name: GetServerConfig
  type: http
  seq: 1
}

get {
  url: http://{{HOST}}/api/server/config
  body: json
  auth: bearer
}

auth:bearer {
  token: {{token}}
}

body:multipart-form {
  playername: Dalraz
}

settings {
  encodeUrl: true
  timeout: 0
}
