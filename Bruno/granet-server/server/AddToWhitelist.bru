meta {
  name: AddToWhitelist
  type: http
  seq: 1
}

post {
  url: http://{{HOST}}/api/server/set-whitelist-mode/
  body: json
  auth: bearer
}

auth:bearer {
  token: {{token}}
}

body:json {
  {
    "enabled": false
  }
}

body:multipart-form {
  playername: Dalraz
}

settings {
  encodeUrl: true
  timeout: 0
}
