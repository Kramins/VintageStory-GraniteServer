meta {
  name: PostServerConfig_WhiteList
  type: http
  seq: 2
}

post {
  url: http://{{HOST}}/api/server/config
  body: json
  auth: bearer
}

auth:bearer {
  token: {{token}}
}

body:json {
  {
    "whitelistMode": "On"
  }
}

body:multipart-form {
  playername: Dalraz
}

settings {
  encodeUrl: true
  timeout: 0
}
