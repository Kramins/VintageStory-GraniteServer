meta {
  name: GetAssignableRoles
  type: http
  seq: 2
}

get {
  url: http://{{HOST}}/api/permissions/roles
  body: json
  auth: bearer
}

auth:bearer {
  token: {{token}}
}

settings {
  encodeUrl: true
  timeout: 0
}
