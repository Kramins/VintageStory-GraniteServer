meta {
  name: GetAssignableGroups
  type: http
  seq: 3
}

get {
  url: http://{{HOST}}/api/permissions/groups
  body: json
  auth: bearer
}

auth:bearer {
  token: {{token}}
}

settings {
  encodeUrl: true
  timeout: 0
}
