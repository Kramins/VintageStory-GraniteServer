meta {
  name: GetAssignablePrivileges
  type: http
  seq: 1
}

get {
  url: http://{{HOST}}/api/permissions/privileges
  body: json
  auth: bearer
}

auth:bearer {
  token: {{token}}
}

settings {
  encodeUrl: true
  timeout: 0
}
