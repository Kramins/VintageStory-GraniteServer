meta {
  name: GetWorldBounds
  type: http
  seq: 1
}

get {
  url: http://{{HOST}}/api/worldmap/{{serverId}}/bounds
  body: none
  auth: bearer
}

auth:bearer {
  token: {{token}}
}

settings {
  encodeUrl: true
  timeout: 0
}
