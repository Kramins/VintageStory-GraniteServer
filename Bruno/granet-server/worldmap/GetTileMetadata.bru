meta {
  name: GetTileMetadata
  type: http
  seq: 3
}

get {
  url: http://{{HOST}}/api/worldmap/{{serverId}}/tiles/{{chunkX}}/{{chunkZ}}/metadata
  body: none
  auth: bearer
}

auth:bearer {
  token: {{token}}
}

settings {
  encodeUrl: true
  timeout: 0
}
