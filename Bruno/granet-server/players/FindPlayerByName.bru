meta {
  name: FindPlayerByName
  type: http
  seq: 7
}

get {
  url: http://{{HOST}}/api/players/find?name={{name}}
  body: json
  auth: bearer
}

auth:bearer {
  token: {{token}}
}

params:query {
  name: {{name}}
}

vars:pre-request {
  name: Dalraz
}

settings {
  encodeUrl: true
  timeout: 0
}
