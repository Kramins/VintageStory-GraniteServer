meta {
  name: GetEvents
  type: http
  seq: 3
}

get {
  url: http://{{HOST}}/api/events
  body: json
  auth: bearer
}

auth:bearer {
  token: {{token}}
}

body:multipart-form {
  playername: Dalraz
}

settings {
  encodeUrl: true
  timeout: 0
}
